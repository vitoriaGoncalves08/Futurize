import React, { useState } from 'react';
import './Settings.css'; // Vamos adicionar algumas classes CSS

function Settings() {
  return (
    <div className="container">
      <div className="config-profile"></div>
    </div>
  );
}

export default Settings;